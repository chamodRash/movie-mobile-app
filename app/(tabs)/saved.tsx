import React from "react";
import { Text, View } from "react-native";

const Saved = () => {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="text-5xl font-bold text-light-300">Saved</Text>
    </View>
  );
};

export default Saved;
